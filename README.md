# Optical-Character-Recognition
This project extracts text from scanned patient assessment forms (JPEG/PDF), parses the data, and stores it in a SQL database.

## Setup
1. Install dependencies:
   ```bash
   pip install pytesseract pillow PyMuPDF
